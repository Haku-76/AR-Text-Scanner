using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OpenJTalkTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        OpenJTalk.Speak("The World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
